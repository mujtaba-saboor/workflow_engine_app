<%= render partial: 'status_badge', locals: { status: issue.status } %>
<%= render partial: 'priority_badge', locals: { priority: issue.priority } %>
<%= render partial: 'issue_type_badge', locals: { issue_type: issue.issue_type } %>