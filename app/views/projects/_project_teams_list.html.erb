<h3 align = center><%= t('projects.all_teams') %></h3>
<% if can? :new_team_for_project, Project %>
  <%= link_to t('projects.add_team'), new_team_for_project_project_path(@project), {class: 'btn btn-success', remote: true, data: { toggle: "modal", target: '#my_id_add_team_to_project_modal'}}%> 
<% end %>
<br><br>

<ul class="list-group">
  <% @project.teams.each do |team| %>
    <li class="list-group-item d-flex justify-content-between align-items-center">
    <%= team.name%>
    <% if can? :remove_user_from_project, @project %>
      <%= link_to t('projects.remove_team'), remove_team_from_project_project_path(@project, team: team), method: :delete, class: 'badge badge-danger'%>
    <% end %>
    </li>
  <% end %>
</ul>
<div id = "my_id_add_team_to_project_modal" class="modal fade" tabindex="-1" role="dialog">
  <div class="modal-dialog modal-dialog-centered" role="document">
    <div class="modal-content">
  </div>
  </div>
</div>