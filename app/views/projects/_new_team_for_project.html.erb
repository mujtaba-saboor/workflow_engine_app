<div class="modal-header">
  <h5 class="modal-title"><%= t('projects.add_team') %></h5>
  <button type="button" class="close" data-dismiss="modal" aria-label="Close">
    <span aria-hidden="true">&times;</span>
  </button>
</div>
<div class="modal-body">
  <% project_teams  = @project.available_teams %>
  <% if project_teams.present? %>
    <%= form_with model: @project, url: add_team_to_project_project_path do |f| %>
      <div class="worflow-form">
        <div class="p-t-15 m-r-10">
          <div class="col-12">
            <div class="row no-gutters d-flex justify-content-between">
              <div class="d-flex align-items-end col-sm-4">
                <%= f.label t('projects.available_teams') %>
              </div>
              <div class="col-sm-8 d-flex justify-content-end">
                <%= f.collection_select :team, project_teams, :id, :name, {}, class: 'form-input-area' %>
              </div>
            </div>
          </div>
        </div>
        <div class="p-t-15 m-r-10">
          <div class="col-sm-12 d-flex justify-content-center">
            <%= f.submit t('projects.add_team'), class: 'my-primary-btn form-submit-btn' %>
          </div>
        </div>
      </div>
    <% end %>
  <% else %>
    <p><%= t('projects.no_available_team') %></p>
  <% end %>
</div>