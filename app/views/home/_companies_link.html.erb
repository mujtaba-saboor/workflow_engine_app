<div class="my-4">
  <% unless companies.empty? %>
    <h4><%= t('home.your_companies') %></h4>
    <div class="row"> 
      <% companies.each do |company| %>
        <%= render 'company_link', company: company %>
      <% end %>
    </div>
  <% end %>
</div>